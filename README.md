# vibe_coding_example

Databricks Asset Bundle for a medallion-style weather pipeline (bronze/silver/gold).

## Quick start
1. Authenticate: `databricks auth describe --profile fevm_test`
2. Deploy: `databricks bundle deploy`
3. Run: `databricks bundle run weather_pipeline_job`
